import { test, expect } from './fixtures';
import { TestData } from '@helpers/testData';

test.describe('Payment Tests @regression', () => {
  test('should process payment successfully', async ({ page }) => {
    // Add payment test implementation
  });
});
